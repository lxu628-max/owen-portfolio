# Owen 个人作品集网站 - 项目文档

## 项目概述

为 Owen（上海耀中 YCIS 在读高中生）创建的个人作品集网站，用于展示大学申请背景。

- **域名**：aboutowen.cn（计划中）
- **当前访问地址**：https://owen-portfolio-production.up.railway.app
- **后台管理**：https://owen-portfolio-production.up.railway.app/admin
- **GitHub 仓库**：https://github.com/lxu628-max/owen-portfolio（Public）

---

## 技术栈

- **后端**：Node.js + Express
- **数据库**：SQLite（better-sqlite3）
- **前端**：原生 HTML/CSS/JavaScript（SPA 单页应用）
- **认证**：JWT（JSON Web Token）
- **部署**：Railway.app

---

## 功能模块

### 前台展示（5个板块）

1. **首页**
   - 左侧：个人介绍 + 轮播图
   - 右侧：动态/更新（5条最新内容）

2. **关于我**
   - 个人背景、申请目标、专业方向
   - 学生领导参与、个人特质

3. **体育（竞技）**
   - 游泳比赛记录、成绩、成长轨迹
   - 支持详情页展示

4. **学术成就**
   - IB 课程、学术竞赛、项目经历
   - 支持详情页展示

5. **西藏项目**
   - 社区服务与志愿活动
   - 时间线展示

6. **社团活动**
   - 社团参与、领导经历、机器人项目

### 后台管理

- **访问路径**：/admin
- **默认账号**：admin / admin123
- **功能**：
  - 轮播图管理（6个板块）
  - 首页动态管理
  - 各板块内容编辑
  - 图片上传
  - 网站设置（名称、标题、副标题、页脚、联系邮箱）
  - 修改密码

---

## ⚠️ 重要：数据存储问题

### 当前问题

Railway 免费版**没有持久化存储**，每次重新部署时：
- 数据库会被重置（密码回到 admin/admin123）
- 上传的图片会丢失
- 编辑的所有内容会丢失

### 解决方案

**方案1：Railway 持久化卷（推荐）**
- 升级到 Railway 付费版（$5/月）
- 在 Railway 项目设置 → Volumes 添加持久化卷
- 挂载路径：
  - `/app/db` - 数据库文件
  - `/app/uploads` - 上传的图片

**方案2：迁移到其他平台**
- Render.com（免费版支持持久化）
- 需要修改部署配置

---

## 目录结构

```
owen-portfolio/
├── server.js              # 后端主服务（API + 数据库）
├── package.json           # Node.js 依赖配置
├── .gitignore            # Git 忽略文件
├── middleware/
│   └── auth.js           # JWT 认证中间件
├── public/               # 前台静态文件
│   ├── index.html        # SPA 主页面
│   ├── css/
│   │   └── style.css     # 前台样式
│   └── js/
│       └── app.js        # 前台交互逻辑
├── admin/                # 后台管理
│   ├── index.html        # 登录页面
│   ├── dashboard.html    # 管理面板
│   ├── css/
│   │   └── admin.css     # 后台样式
│   └── js/
│       └── admin.js      # 后台逻辑
├── db/                   # SQLite 数据库（被 gitignore）
│   └── database.sqlite
└── uploads/              # 上传的图片（被 gitignore）
```

---

## 部署流程

### 首次部署

1. 代码推送到 GitHub（已完成）
2. Railway 自动从 GitHub 拉取并部署
3. 访问 Railway 分配的域名

### 更新代码

```bash
cd ~/Desktop/owen-portfolio
# 修改文件后
git add .
git commit -m "描述修改内容"
git push
```

Railway 会自动检测更新并重新部署（约1-2分钟）。

---

## 本地开发

### 安装依赖

```bash
npm install
```

### 启动服务

```bash
npm start
```

访问 http://localhost:3000

### 环境变量（可选）

- `PORT`：服务端口（默认 3000）
- `JWT_SECRET`：JWT 密钥（默认 owen-portfolio-secret-key-2024）

---

## API 接口

### 公开接口

- `GET /api/news` - 获取首页动态
- `GET /api/section/:key` - 获取板块内容
- `GET /api/carousel/:section` - 获取轮播图
- `GET /api/sports` - 获取运动记录
- `GET /api/academic` - 获取学术项目
- `GET /api/tibet` - 获取西藏活动
- `GET /api/clubs` - 获取社团活动
- `GET /api/settings` - 获取网站设置

### 管理接口（需要 JWT 认证）

- `POST /api/admin/login` - 登录
- `POST /api/admin/change-password` - 修改密码
- `POST /api/admin/news` - 保存动态
- `POST /api/admin/section/:key` - 保存板块内容
- `POST /api/admin/carousel/:section` - 保存轮播图
- `POST /api/admin/upload` - 上传图片

---

## 已修复的问题

1. **密码修改 token 错误**（2026-07-08）
   - 问题：修改密码时使用错误的 localStorage key
   - 修复：admin.js 中使用正确的 token 变量

2. **页脚不显示联系邮箱**（2026-07-08）
   - 问题：后台有联系邮箱设置，但前台没有展示
   - 修复：在页脚添加邮箱显示区域

3. **网站名称不显示**（2026-07-08）
   - 问题：后台有网站名称设置，但前台导航栏和标题没有更新
   - 修复：更新导航栏 logo 和浏览器标题

4. **菜单顺序调整**（2026-07-08）
   - 将轮播图管理移到第一位
   - 调整其他菜单项顺序

---

## 待优化功能

- [ ] 移动端适配优化
- [ ] 图片懒加载
- [ ] SEO 优化
- [ ] 多语言支持（中英文切换）
- [ ] 社交分享功能
- [ ] 访客统计

---

## 联系与支持

如有问题，联系项目管理员。

---

**文档创建时间**：2026-07-08  
**最后更新**：2026-07-08
